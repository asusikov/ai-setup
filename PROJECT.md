# TUI app to operate with GitLab

This is a console app with a terminal user interface to operate pipelines and merge requests. The goal of the app is to provide a lightweight and fast interface to view, run pipelines and review merge requests.  
